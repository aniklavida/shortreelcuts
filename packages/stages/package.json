{
  "name": "@shortreelcuts/stages",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The five stage runner stubs (script, voice, footage, align) plus the one real stage, compose (via @shortreelcuts/render). Framework-free on purpose — both the browser decision-sheet package and the worker process import the same runners from here.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@shortreelcuts/plan": "^0.1.0",
    "@shortreelcuts/render": "^0.1.0"
  }
}
