{
  "name": "@shortreelcuts/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The pipeline runner. Executes a plan through the stage graph in a worker process, not a web request, and resumes a job from its last completed stage after a crash, a restart, or a browser refresh — docs/SPEC.md §10.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@shortreelcuts/db": "^0.1.0",
    "@shortreelcuts/plan": "^0.1.0",
    "@shortreelcuts/stages": "^0.1.0",
    "pg-boss": "^12.31.1"
  }
}
